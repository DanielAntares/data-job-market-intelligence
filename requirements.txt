# Runtime dependencies — also what Streamlit Community Cloud installs to deploy.
requests>=2.31
pandas>=2.2             # groupby(...).apply(include_groups=False) needs 2.2
pyarrow>=15.0          # parquet engine for the warehouse
PyYAML>=6.0            # config.yaml
python-dateutil>=2.8   # robust date parsing across sources
beautifulsoup4>=4.12   # strip HTML out of job descriptions
python-dotenv>=1.0     # load API keys from a git-ignored .env
matplotlib>=3.8        # charts (Phase 3)
scikit-learn>=1.6      # salary model; GroupKFold(shuffle=, random_state=) needs 1.6
streamlit>=1.49        # dashboard (Phase 5); uses width="stretch" API
